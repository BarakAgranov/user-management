server:
  port: 8082

spring:
  application:
    name: role
  datasource:
    password: 9088007
    url: jdbc:postgresql://localhost:5432/role
    username: barak
    jpa:
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
          format_sql: true
      show-sql: true