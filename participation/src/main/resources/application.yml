server:
  port: 8084

spring:
  application:
    name: participation
  datasource:
    password: 9088007
    url: jdbc:postgresql://localhost:5432/participation
    username: barak
    jpa:
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
          format_sql: true
      show-sql: true