server:
  port: 8083

spring:
  application:
    name: permission
  datasource:
    password: 9088007
    url: jdbc:postgresql://localhost:5432/permission
    username: barak
    jpa:
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
          format_sql: true
      show-sql: true